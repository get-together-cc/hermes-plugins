name: alibaba-quota-retry
version: 1.0.0
description: 阿里云 token-plan 的 429 insufficient_quota 多为短窗口 TPM 触顶（分钟级自动恢复），重分类为 rate_limit 走延迟重试，而非 billing 直接断流。
provides_hooks:
  - transform_api_error_classification
